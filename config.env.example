# Configurazione Citofono VoIP
# Copia questo file in config.env e modifica i valori secondo la tua installazione
#
#   cp config.env.example config.env
#
# ATTENZIONE: config.env contiene credenziali e NON va committato nel repository.

# GPIO Pins (BCM)
PIN_SUONERIA=17
PIN_RELE_PORTONE=27
PIN_LED_STATO=22

# Configurazione SIP
SIP_USERNAME=2000
SIP_PASSWORD=changeme
SIP_DOMAIN=centralino.ponsacco.local
SIP_PORT=5060

# Numero da chiamare quando suona il citofono
NUMERO_DA_CHIAMARE=6400

# Codice DTMF per apertura portone
# NOTA: evitare codici che iniziano con * perché i PBX Grandstream
# li interpretano come feature code interni e non li rilanciano al chiamante.
DTMF_APRI_PORTONE=99

# Timing (secondi)
DURATA_APERTURA=2
TIMEOUT_CHIAMATA=60

# Audio (verifica con 'aplay -l' e 'arecord -l')
# AUDIO_PLAY_DEVICE=hw:1,0
# AUDIO_REC_DEVICE=hw:1,0

# Log
# LOG_FILE=/var/log/citofono-voip.log
